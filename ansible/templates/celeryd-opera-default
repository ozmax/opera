CELERYD_NODES="{{ project_name }}"
CELERY_BIN="{{ virtualenv }}/bin/celery"
CELERYD_CHDIR="{{ deployment_path }}"
CELERY_CREATE_DIRS=1
CELERYD_LOG_FILE="/var/log/{{ celery_service }}/%N.log"
CELERYD_PID_FILE="/var/run/{{ celery_service }}/%N.pid"
CELERYD_USER="{{ unix_user }}"
CELERYD_GROUP="{{ unix_user }}"
CELERY_APP="{{ project_name }}"
